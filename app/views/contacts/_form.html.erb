<%= semantic_form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<%= f.inputs do%>
	  <div class="field">
	    <%= f.input :title, :label => 'title', :input_html => {:value => params[:title]} %><br />
	    
	    <%= f.input :name, :label => "name" %><br />
	    
	    <%= f.input :email, :label => "email" %><br />
	      
	    <%= f.input :phone, :label => "phone" %><br />
	      
	    <%= f.input :message, :label => "message" %><br />    
	  </div>
	<% end %>
	<%= f.buttons do%>
	  <div class="actions">
	    <%= f.commit_button %>
	  </div>
	<% end %>
<% end %>
